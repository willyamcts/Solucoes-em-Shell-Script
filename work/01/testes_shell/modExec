

verifyModeExec() {
echo "$1 - Verificando modo de execucao"
echo $(pwd)
#	[ -z $1 ] && selectModeExecution 

o1="Range de IP"
o2="Address list"

	case $1 in
		"$o1") $2
			;;
		"$o2") $3
			;;
	esac

}